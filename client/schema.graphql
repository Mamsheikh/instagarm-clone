type Query {
  searchUser(input: String): [User]
}

type User {
  address: String
  bio: String
  email: String!
  id: String!
  image: String
  isAdmin: Boolean!
  name: String!
  phone: String
  username: String
  website: String
}
